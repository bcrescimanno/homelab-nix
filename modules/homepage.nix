# modules/homepage.nix — Homepage dashboard
#
# Homepage is a self-hosted dashboard that aggregates links and status
# widgets for all homelab services. Runs on mirkwood (4GB Pi 5).
#
# TODO: Implement container config once deployed.

{ config, pkgs, lib, ... }:

{
  # placeholder — no services configured yet
}
