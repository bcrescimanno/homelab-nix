# modules/proxy.nix — Nginx Proxy Manager
#
# Nginx Proxy Manager provides a web UI for managing reverse proxy rules,
# SSL certificates, and access lists. Runs on rivendell (8GB Pi 5).
#
# TODO: Implement container config once deployed.

{ config, pkgs, lib, ... }:

{
  # placeholder — no services configured yet
}
